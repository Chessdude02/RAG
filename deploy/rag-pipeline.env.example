# Copy this file to /etc/rag-pipeline/rag-pipeline.env on the server.
# Then: sudo chown raguser:raguser /etc/rag-pipeline/rag-pipeline.env
#       sudo chmod 600 /etc/rag-pipeline/rag-pipeline.env
# Never commit the real file (with a real key) to git.

ANTHROPIC_API_KEY=sk-ant-your-key-here
